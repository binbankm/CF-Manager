# =============================================================================
# Cloudflare Manager - 环境变量配置
# =============================================================================
#
# 🔒 安全提示:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 修改所有包含 "CHANGE-THIS" 的值
# 3. 不要提交 .env 文件到版本控制系统
#
# 生成安全密钥的方法:
#   JWT_SECRET:      openssl rand -base64 32
#   ENCRYPTION_KEY:  openssl rand -hex 16
# =============================================================================

# -----------------------------------------------------------------------------
# 应用基础配置
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=5143

# -----------------------------------------------------------------------------
# 安全密钥配置 (⚠️ 生产环境必须修改！)
# -----------------------------------------------------------------------------

# JWT 密钥 - 用于用户认证token签名
# 要求: 至少 32 个字符的随机字符串
# 生成: openssl rand -base64 32
JWT_SECRET=CHANGE-THIS-TO-RANDOM-STRING-MIN-32-CHARS

# 数据加密密钥 - 用于加密 Cloudflare API Token
# 要求: 必须是 32 个字符（64位十六进制）
# 生成: openssl rand -hex 16
ENCRYPTION_KEY=CHANGE-THIS-32-CHAR-HEX-STRING

# -----------------------------------------------------------------------------
# 数据库配置
# -----------------------------------------------------------------------------
DATABASE_PATH=/app/data/database.sqlite

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# 跨域配置（如果需要从不同域名访问）
# CORS_ORIGIN=https://your-domain.com

